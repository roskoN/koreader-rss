local _ = require("gettext")

return {
    fullname = _("RSS Reader integration probe"),
    description = _([[Validates the Rust, SQLite, subprocess, and HTML boundaries required by the offline RSS reader.]]),
}
